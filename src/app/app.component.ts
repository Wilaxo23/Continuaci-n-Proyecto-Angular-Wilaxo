import { RouterOutlet } from '@angular/router';
import { Component } from '@angular/core';

export class AppModule {}
@Component({
  selector: 'app-root',
  standalone: false,
  templateUrl: './app.component.html',
})
export class AppComponent {
  public title: string = 'vamos ostia arribaaaa';
}
